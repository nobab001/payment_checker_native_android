# Claude Code — PayChek

This project’s Claude Code instructions live in the repo root:

**`CLAUDE.md`** (authoritative)  
**`.claude/rules/`** (same content as `.cursor/rules/` — keep in sync)

When updating Cursor rules under `.cursor/rules/`, re-sync to `.claude/rules/` so both agents behave the same.
